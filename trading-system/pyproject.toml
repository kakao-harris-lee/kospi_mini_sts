[project]
name = "trading-system"
version = "0.1.0"
description = "Redis Streams 기반 실시간 트레이딩 시스템"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}

dependencies = [
    "redis>=5.0.0",
    "numpy>=1.24.0",
    "pandas>=2.0.0",
]

[project.optional-dependencies]
ml = [
    "torch>=2.0.0",
]
db = [
    "clickhouse-connect>=0.7.0",
]
dev = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
    "black>=23.0.0",
    "ruff>=0.1.0",
    "mypy>=1.0.0",
]
all = [
    "trading-system[ml,db,dev]",
]

[project.scripts]
collector = "src.collector.data_collector:main"
processor = "src.processor.feature_processor:main"
db-logger = "src.db_logger.db_logger:main"
prediction = "src.prediction.prediction_engine:main"
strategy = "src.strategy.strategy_manager:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src", "config"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_ignores = true
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-v --tb=short"
